# 앞단(DSM nginx)이 TLS 를 종료하고 X-Forwarded-Proto: https 를 붙여 보낸다.
# 그 값을 그대로 이어서 넘긴다. $scheme 을 쓰면 여기서는 항상 http 라
# 원래 프로토콜 정보가 지워지고, 백엔드가 같은 출처의 요청을 CORS 로
# 오판해 403 을 내게 된다.
map $http_x_forwarded_proto $client_proto {
    default $http_x_forwarded_proto;
    ''      $scheme;
}

server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip 압축
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;

    # 정적 파일 캐싱
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # API 프록시: 같은 docker-compose 네트워크의 backend 서비스로 전달
    # proxy_pass 끝에 슬래시를 붙이면 /api 접두어가 제거되므로 반드시 생략할 것
    # (백엔드 엔드포인트가 /api/v1/... 이므로 경로를 그대로 넘겨야 함)
    location /api/ {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $client_proto;
        proxy_cache_bypass $http_upgrade;
    }

    # SPA 라우팅 지원 (모든 요청을 index.html로)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 보안 헤더
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}
